server:
  port: 8123
  address: 0.0.0.0

spring:
  application:
    name: music-proxy-server
  threads:
    virtual:
      enabled: true
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /index.html
